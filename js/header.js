/**
 * Header management for HEMOPI blood donation system
 * Handles authentication state in header
 */

import { initAuthStateMonitoring } from './auth-manager.js';

// Initialize authentication state monitoring for header
initAuthStateMonitoring();
